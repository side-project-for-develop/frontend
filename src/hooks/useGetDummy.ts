// import { useQuery } from "@tanstack/react-query";
// import { apis } from "@/api/endpoints/UserAPIs";
// import { queryKeys } from "@/api/keys";

// const useGetDummy = () => {
//   const fetcher = async () => {
//     const { data } = await apis.fetchDummy();
//     return data;
//   };

//   return useQuery(queryKeys.DUMMY, fetcher);
// };

// export default useGetDummy;
